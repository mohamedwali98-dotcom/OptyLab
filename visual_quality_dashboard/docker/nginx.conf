server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # React Router SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Gzip for faster delivery
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
}